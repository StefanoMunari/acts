package Shared.Random is
   --  function that generates random numbers between a lower and an upper bound
   procedure Generate_Random (Lower_Bound, Upper_Bound : in     Positive;
                              Result                   :    out Positive);

end Shared.Random;
